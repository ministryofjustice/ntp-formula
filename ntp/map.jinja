{% set ntp = salt['grains.filter_by']({
    'Default': {
        'version': '',
        'servers': ['0.pool.ntp.org', '1.pool.ntp.org',
                    '2.pool.ntp.org', '3.pool.ntp.org'],
    },
    'Ubuntu-14.04': {
        'version': '',
        'servers': ['0.pool.ntp.org', '1.pool.ntp.org',
                    '2.pool.ntp.org', '3.pool.ntp.org'],
    },
}, grain='osfinger', merge=salt['pillar.get']('ntp', {}), default='Default' ) %}
